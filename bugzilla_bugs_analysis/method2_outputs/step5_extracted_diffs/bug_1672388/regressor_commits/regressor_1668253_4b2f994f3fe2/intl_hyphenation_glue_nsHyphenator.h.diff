# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: intl/hyphenation/glue/nsHyphenator.h
# Commit: 4b2f994f3fe2
# Full Hash: 4b2f994f3fe2cad3895cc4d5044a3466a2d5eda4
# Author: Jonathan Kew <jkew@mozilla.com>
# Date: 2020-10-12 15:27:19
# Regressor Bug: 1668253
# File Overlap Count: 1
# Description:
#   Bug 1668253 - Convert hyphenation from ipc::SharedMemoryBasic to base::SharedMemory, and freeze blocks before sharing to content processes. r=heycam
#   
#   Differential Revision: https://phabricator.services.mozilla.com/D91945
# ==============================================================================

diff -r bc5b6dcb7057 -r 4b2f994f3fe2 intl/hyphenation/glue/nsHyphenator.h
--- a/intl/hyphenation/glue/nsHyphenator.h	Mon Oct 12 10:23:27 2020 +0000
+++ b/intl/hyphenation/glue/nsHyphenator.h	Mon Oct 12 10:24:06 2020 +0000
@@ -6,7 +6,7 @@
 #ifndef nsHyphenator_h__
 #define nsHyphenator_h__
 
-#include "mozilla/ipc/SharedMemoryBasic.h"
+#include "base/shared_memory.h"
 #include "mozilla/UniquePtr.h"
 #include "mozilla/Variant.h"
 #include "nsCOMPtr.h"
@@ -42,8 +42,7 @@
   nsresult Hyphenate(const nsAString& aText, nsTArray<bool>& aHyphens);
 
   void ShareToProcess(base::ProcessId aPid,
-                      mozilla::ipc::SharedMemoryBasic::Handle* aOutHandle,
-                      uint32_t* aOutSize);
+                      base::SharedMemoryHandle* aOutHandle, uint32_t* aOutSize);
 
  private:
   ~nsHyphenator() = default;
@@ -52,7 +51,7 @@
                      nsTArray<bool>& aHyphens);
 
   mozilla::Variant<const void*,  // raw pointer to uncompressed omnijar data
-                   RefPtr<mozilla::ipc::SharedMemoryBasic>,  // shmem block
+                   mozilla::UniquePtr<base::SharedMemory>,  // shmem block
                    mozilla::UniquePtr<const HyphDic>  // loaded by mapped_hyph
                    >
       mDict;
