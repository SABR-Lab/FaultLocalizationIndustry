# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: dom/media/mediacapabilities/MediaCapabilities.cpp
# Commit: 1932f2f34993
# Full Hash: 1932f2f3499302f712bb680096d51478e53c99ed
# Author: Jean-Yves Avenard <jyavenard@mozilla.com>
# Date: 2020-10-21 09:51:28
# Regressor Bug: 1595994
# File Overlap Count: 1
# Description:
#   Bug 1595994 - P6: Change Supports to take SupportDecoderParams. r=kamidphish
#   
#   This is a subset of the parameters passed via CreateDecoderParams and is so
#   Supports() calls have access to KnowsCompositor and Options when determining
#   if decoding is supported.
# ==============================================================================

diff -r 0606794a9e8c -r 1932f2f34993 dom/media/mediacapabilities/MediaCapabilities.cpp
--- a/dom/media/mediacapabilities/MediaCapabilities.cpp	Tue Oct 20 23:24:48 2020 +0000
+++ b/dom/media/mediacapabilities/MediaCapabilities.cpp	Tue Oct 20 23:26:25 2020 +0000
@@ -215,7 +215,8 @@
       // There's no need to create an audio decoder has we only want to know if
       // such codec is supported
       RefPtr<PDMFactory> pdm = new PDMFactory();
-      if (!pdm->Supports(*config, nullptr /* decoder doctor */)) {
+      SupportDecoderParams params{*config};
+      if (!pdm->Supports(params, nullptr /* decoder doctor */)) {
         auto info = MakeUnique<MediaCapabilitiesInfo>(
             false /* supported */, false /* smooth */,
             false /* power efficient */);