# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: testing/web-platform/meta/content-security-policy/inheritance/history-iframe.sub.html.ini
# Commit: 85883a720dab
# Full Hash: 85883a720dab61e82bcdb7391236dc06427466ee
# Author: moz-wptsync-bot <wptsync@mozilla.com>
# Date: 2021-04-07 15:47:02
# Regressor Bug: 1702830
# File Overlap Count: 1
# Description:
#   Bug 1702830 - [wpt-sync] Update web-platform-tests to 336ec39f06d15863a4af6b2aa6592530b37a5aee, a=testonly
#   
#   MANUAL PUSH: wpt sync bot
#   
#   wpt-head: 336ec39f06d15863a4af6b2aa6592530b37a5aee
# ==============================================================================

diff -r acef12d74007 -r 85883a720dab testing/web-platform/meta/content-security-policy/inheritance/history-iframe.sub.html.ini
--- a/testing/web-platform/meta/content-security-policy/inheritance/history-iframe.sub.html.ini	Fri Apr 02 22:03:46 2021 +0000
+++ b/testing/web-platform/meta/content-security-policy/inheritance/history-iframe.sub.html.ini	Fri Apr 02 22:06:52 2021 +0000
@@ -1,18 +1,24 @@
 [history-iframe.sub.html]
   expected:
-    if os == "linux" and webrender: ["OK", "TIMEOUT"]
+    if (os == "linux") and webrender: ["OK", "TIMEOUT"]
     if os == "mac": ["OK", "TIMEOUT"]
   [History navigation in iframe: "about:blank" document is navigated back from history same-origin.]
     expected:
-      if (os == "linux") and webrender: ["FAIL", "PASS", "TIMEOUT"]
+      if (os == "linux") and not webrender and not debug and (processor == "x86_64"): ["PASS", "FAIL"]
+      if (os == "linux") and not webrender and not debug and (processor == "x86"): ["PASS", "FAIL"]
+      if (os == "linux") and webrender and debug and fission: ["PASS", "FAIL", "TIMEOUT"]
+      if (os == "linux") and webrender and debug and not fission: ["PASS", "FAIL", "TIMEOUT"]
+      if (os == "linux") and not webrender and debug: ["PASS", "FAIL"]
+      if (os == "linux") and webrender and not debug: ["PASS", "FAIL", "TIMEOUT"]
       if (os == "win") and (processor == "x86") and debug: ["PASS", "FAIL"]
-      if (os == "linux") and not webrender: ["PASS", "FAIL"]
       if os == "mac": ["PASS", "TIMEOUT"]
 
   [History navigation in iframe: "about:blank" document is navigated back from history cross-origin.]
     expected:
-      if (os == "linux") and webrender: ["PASS", "NOTRUN", "TIMEOUT"]
-      if os == "mac": ["PASS", "NOTRUN", "TIMEOUT"]
+      if (os == "linux") and webrender and fission and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
+      if (os == "linux") and webrender and fission and debug: ["PASS", "FAIL", "TIMEOUT", "NOTRUN"]
+      if (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT", "NOTRUN"]
+      if os == "mac": ["PASS", "TIMEOUT", "NOTRUN"]
 
   [History navigation in iframe: blob URL document is navigated back from history same-origin.]
     expected: