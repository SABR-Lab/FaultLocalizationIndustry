# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: dom/media/webrtc/libwebrtcglue/VideoConduit.h
# Commit: ef817155231f
# Full Hash: ef817155231f6fed522ce793cbc82b7be019de56
# Author: Andreas Pehrson <apehrson@mozilla.com>
# Date: 2021-11-01 18:17:33
# Regressor Bug: 1654112
# File Overlap Count: 5
# Description:
#   Bug 1654112 - Constify conduit getters. r=ng
#   
#   Differential Revision: https://phabricator.services.mozilla.com/D122244
# ==============================================================================

diff -r 310fdf39b051 -r ef817155231f dom/media/webrtc/libwebrtcglue/VideoConduit.h
--- a/dom/media/webrtc/libwebrtcglue/VideoConduit.h	Sun Apr 18 21:25:11 2021 +0200
+++ b/dom/media/webrtc/libwebrtcglue/VideoConduit.h	Tue Aug 10 14:58:34 2021 +0200
@@ -205,7 +205,7 @@
                        const rtc::VideoSinkWants& wants) override;
   void RemoveSink(rtc::VideoSinkInterface<webrtc::VideoFrame>* sink) override;
 
-  bool HasCodecPluginID(uint64_t aPluginID) override;
+  bool HasCodecPluginID(uint64_t aPluginID) const override;
 
   void Shutdown() override;
 
@@ -227,10 +227,10 @@
   // Necessary Init steps on the Call thread.
   void InitCall();
 
-  std::vector<uint32_t> GetLocalSSRCs() override;
+  std::vector<uint32_t> GetLocalSSRCs() const override;
 
   // Any thread.
-  bool GetRemoteSSRC(uint32_t* ssrc) override;
+  bool GetRemoteSSRC(uint32_t* ssrc) const override;
 
   // Call thread only.
   bool SetLocalSSRCs(const std::vector<uint32_t>& ssrcs,
@@ -308,7 +308,7 @@
   // thread.
   const nsCOMPtr<nsISerialEventTarget> mStsThread;
 
-  Mutex mMutex;
+  mutable Mutex mMutex;
 
   // Decoder factory used by mRecvStream when it needs new decoders. This is
   // not shared broader like some state in the WebrtcCallWrapper because it
