# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: editor/libeditor/EditorBase.h
# Commit: 3ea6eaf0dc16
# Full Hash: 3ea6eaf0dc166b86dfd68dd10738774991419795
# Author: Masayuki Nakano <masayuki@d-toybox.com>
# Date: 2022-08-04 09:46:07
# Regressor Bug: 1774704
# File Overlap Count: 1
# Description:
#   Bug 1774704 - part 9: Get rid of `EditorBase::TopLevelEditSubActionData::mNewBlockElement` due to unused r=m_kato
#   
#   Differential Revision: https://phabricator.services.mozilla.com/D152982
# ==============================================================================

diff -r 8896439912aa -r 3ea6eaf0dc16 editor/libeditor/EditorBase.h
--- a/editor/libeditor/EditorBase.h	Thu Aug 04 05:31:12 2022 +0000
+++ b/editor/libeditor/EditorBase.h	Thu Aug 04 05:39:15 2022 +0000
@@ -790,9 +790,6 @@
   struct MOZ_STACK_CLASS TopLevelEditSubActionData final {
     friend class AutoEditActionDataSetter;
 
-    // If we have created a new block element, set to it.
-    RefPtr<Element> mNewBlockElement;
-
     // Set selected range before edit.  Then, RangeUpdater keep modifying
     // the range while we're changing the DOM tree.
     RefPtr<RangeItem> mSelectedRange;
@@ -882,7 +879,6 @@
       if (!mSelectedRange) {
         return;
       }
-      mNewBlockElement = nullptr;
       mSelectedRange->Clear();
       mChangedRange->Reset();
       if (mCachedInlineStyles.isSome()) {