# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: layout/base/nsFrameIterator.cpp
# Commit: 8bf69c3fd814
# Full Hash: 8bf69c3fd81419337cb93f96020bb5e88a333d32
# Author: Jan Varga <Jan.Varga@gmail.com>
# Date: 2012-08-07 13:39:24
# Regressor Bug: 779684
# File Overlap Count: 1
# Description:
#   Bug 779684 - Move FrameIteratorFlags to nsFrameIterator class. r=roc
# ==============================================================================

diff -r 78aaaf0d8185 -r 8bf69c3fd814 layout/base/nsFrameIterator.cpp
--- a/layout/base/nsFrameIterator.cpp	Mon Aug 06 14:51:33 2012 -0600
+++ b/layout/base/nsFrameIterator.cpp	Mon Aug 06 20:27:04 2012 -0400
@@ -14,9 +14,9 @@
   , mOffEdge(0)
   , mType(aType)
 {
-  mFollowOOFs = (aFlags & FrameIteratorFlags::FLAG_FOLLOW_OUT_OF_FLOW) != 0;
-  mLockScroll = (aFlags & FrameIteratorFlags::FLAG_LOCK_SCROLL) != 0;
-  mVisual = (aFlags & FrameIteratorFlags::FLAG_VISUAL) != 0;
+  mFollowOOFs = (aFlags & FLAG_FOLLOW_OUT_OF_FLOW) != 0;
+  mLockScroll = (aFlags & FLAG_LOCK_SCROLL) != 0;
+  mVisual = (aFlags & FLAG_VISUAL) != 0;
   if (mFollowOOFs && aStart)
     aStart = nsPlaceholderFrame::GetRealFrameFor(aStart);
   setStart(aStart);