# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: widget/cocoa/OSXNotificationCenter.mm
# Commit: 5b0f82178f63
# Full Hash: 5b0f82178f636ffde1d06fdf400f547fb3deb06c
# Author: Emilio Cobos Álvarez <emilio@crisal.io>
# Date: 2024-03-02 21:55:19
# Regressor Bug: 1883166
# File Overlap Count: 1
# Description:
#   Bug 1883166 - Support SVG images without intrinsic size (just ratio) on macOS menus. r=mac-reviewers,tnikkel
#   
#   Images like the one in comment 1 would end with a zero size otherwise
#   and fail to render. The caller usually knows the size it wants to render
#   into, so use that.
# ==============================================================================

diff -r 99d8110cbb26 -r 5b0f82178f63 widget/cocoa/OSXNotificationCenter.mm
--- a/widget/cocoa/OSXNotificationCenter.mm	Sat Mar 02 07:56:42 2024 +0000
+++ b/widget/cocoa/OSXNotificationCenter.mm	Sat Mar 02 08:50:43 2024 +0000
@@ -544,9 +544,11 @@
 
   NSImage* cocoaImage = nil;
   // TODO: Pass pres context / ComputedStyle here to support context paint
-  // properties
+  // properties.
+  // TODO: Do we have a reasonable size to pass around here?
   nsCocoaUtils::CreateDualRepresentationNSImageFromImageContainer(
-      image, imgIContainer::FRAME_FIRST, nullptr, nullptr, &cocoaImage);
+      image, imgIContainer::FRAME_FIRST, nullptr, nullptr, NSMakeSize(0, 0),
+      &cocoaImage);
   (osxni->mPendingNotification).contentImage = cocoaImage;
   [cocoaImage release];
   ShowPendingNotification(osxni);