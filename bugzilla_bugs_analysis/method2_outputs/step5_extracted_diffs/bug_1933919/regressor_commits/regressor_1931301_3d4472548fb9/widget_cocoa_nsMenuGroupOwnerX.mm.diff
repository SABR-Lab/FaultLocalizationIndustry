# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: widget/cocoa/nsMenuGroupOwnerX.mm
# Commit: 3d4472548fb9
# Full Hash: 3d4472548fb9d48cce00f59066690611ee20ffcb
# Author: Emilio Cobos Álvarez <emilio@crisal.io>
# Date: 2024-11-26 09:36:10
# Regressor Bug: 1931301
# File Overlap Count: 1
# Description:
#   Bug 1931301 - Notify of removals on a stable state, not mid-remove. r=smaug,masayuki,dshin
#   
#   This simplifies some observers, and makes others a bit more subtle, but I think
#   over-all it is an improvement.
#   
# ==============================================================================

diff -r 20a0394d798f -r 3d4472548fb9 widget/cocoa/nsMenuGroupOwnerX.mm
--- a/widget/cocoa/nsMenuGroupOwnerX.mm	Mon Nov 25 17:56:21 2024 +0000
+++ b/widget/cocoa/nsMenuGroupOwnerX.mm	Mon Nov 25 17:58:40 2024 +0000
@@ -92,8 +92,7 @@
   }
 }
 
-void nsMenuGroupOwnerX::ContentRemoved(nsIContent* aChild,
-                                       nsIContent* aPreviousSibling) {
+void nsMenuGroupOwnerX::ContentWillBeRemoved(nsIContent* aChild) {
   nsIContent* container = aChild->GetParent();
   if (!container) {
     return;
@@ -102,8 +101,7 @@
   nsCOMPtr<nsIMutationObserver> kungFuDeathGrip(this);
   nsChangeObserver* obs = LookupContentChangeObserver(container);
   if (obs) {
-    obs->ObserveContentRemoved(aChild->OwnerDoc(), container, aChild,
-                               aPreviousSibling);
+    obs->ObserveContentRemoved(aChild->OwnerDoc(), container, aChild);
   } else if (container != mContent) {
     // We do a lookup on the parent container in case things were removed
     // under a "menupopup" item. That is basically a wrapper for the contents
@@ -112,8 +110,7 @@
     if (parent) {
       obs = LookupContentChangeObserver(parent);
       if (obs) {
-        obs->ObserveContentRemoved(aChild->OwnerDoc(), container, aChild,
-                                   aPreviousSibling);
+        obs->ObserveContentRemoved(aChild->OwnerDoc(), container, aChild);
       }
     }
   }