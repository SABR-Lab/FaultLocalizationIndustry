# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: testing/web-platform/meta/fedcm/lfedcm-identity.discovery.tentative.sub.https.html.ini
# Commit: 425cbe3c033b
# Full Hash: 425cbe3c033bfc25795979513ebd2bb9fda7338d
# Author: moz-wptsync-bot <wptsync@mozilla.com>
# Date: 2024-11-28 21:54:11
# Regressor Bug: 1933829
# File Overlap Count: 1
# Description:
#   Bug 1933829 - [wpt-sync] Update web-platform-tests to 2d5d222739fdf91743f8cbc58094976a613194e7, a=testonly
#   
#   MANUAL PUSH: wpt sync bot
#   
#   wpt-head: 2d5d222739fdf91743f8cbc58094976a613194e7
# ==============================================================================

diff -r 732bcd7ac971 -r 425cbe3c033b testing/web-platform/meta/fedcm/lfedcm-identity.discovery.tentative.sub.https.html.ini
--- a/testing/web-platform/meta/fedcm/lfedcm-identity.discovery.tentative.sub.https.html.ini	Wed Nov 27 22:05:28 2024 +0000
+++ b/testing/web-platform/meta/fedcm/lfedcm-identity.discovery.tentative.sub.https.html.ini	Wed Nov 27 22:05:56 2024 +0000
@@ -1,16 +1,16 @@
 [lfedcm-identity.discovery.tentative.sub.https.html]
   expected:
-    if fission and (os == "win") and not debug and (processor == "x86_64"): TIMEOUT
-    if fission and (os == "linux") and not asan and not tsan: [TIMEOUT, OK]
-    if not fission: OK
+    if (os == "linux") and fission and not asan and not tsan: [TIMEOUT, OK]
+    if (os == "win") and (processor == "x86_64") and not debug: TIMEOUT
+    if (os == "win") and (processor == "x86"): [TIMEOUT, OK]
+    if (os == "linux") and not fission: OK
     [OK, TIMEOUT]
   [Cross-origin identity credential discovery does not resolve with effective store from the wrong origin]
     expected:
       if (os == "linux") and fission and not asan and tsan: [PASS, TIMEOUT]
       if (os == "linux") and fission and not asan and not tsan: [NOTRUN, PASS, TIMEOUT]
       if (os == "win") and (processor == "x86_64") and not debug: [NOTRUN, TIMEOUT]
-      if (os == "win") and (processor == "x86") and debug: [PASS, NOTRUN]
-      if (os == "win") and (processor == "x86") and not debug: [PASS, NOTRUN]
+      if (os == "win") and (processor == "x86"): [TIMEOUT, PASS, NOTRUN]
       if (os == "mac") and debug: [PASS, TIMEOUT]
       if (os == "linux") and not fission: PASS
       [PASS, TIMEOUT, NOTRUN]
@@ -18,7 +18,7 @@
   [Cross-origin identity credential discovery does not resolve with ineffective store from the right origin]
     expected:
       if (os == "linux") and fission and not asan and tsan: PASS
-      if (os == "linux") and fission and not asan and not tsan: [TIMEOUT, NOTRUN]
+      if (os == "linux") and fission and not asan and not tsan: [NOTRUN, TIMEOUT]
       if (os == "win") and not debug and (processor == "x86_64"): [TIMEOUT, PASS, NOTRUN]
       if (os == "mac") and debug: PASS
       if (os == "linux") and not fission: PASS
@@ -26,6 +26,5 @@
 
   [Cross-origin identity credential discovery works using the effectiveQueryURL]
     expected:
-      if (processor == "x86_64") and (os == "linux") and fission and not asan and not tsan: [PASS, TIMEOUT]
-      if (processor == "x86_64") and (os == "win") and not debug: [PASS, TIMEOUT]
-      if (processor == "x86") and not debug: [PASS, TIMEOUT]
+      if (os == "linux") and fission and not asan and not tsan: [TIMEOUT, PASS]
+      if (os == "win") and not debug: [PASS, TIMEOUT]