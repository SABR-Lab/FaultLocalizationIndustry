# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: layout/base/PresShell.h
# Commit: 01cecf5bf7e5
# Full Hash: 01cecf5bf7e5603d5d7ebe4c28d8fdc0a74e6b51
# Author: Emilio Cobos Álvarez <emilio@crisal.io>
# Date: 2025-03-25 21:02:33
# Regressor Bug: 1955697
# File Overlap Count: 1
# Description:
#   Bug 1955697 - Simplify scrolled canvas background painting. r=mstange
#   
#   Always paint the scrolled canvas background if it's CSS-specified.
#   
#   Compute the canvas background upfront so we don't need to walk the
# ==============================================================================

diff -r 892e1da580c2 -r 01cecf5bf7e5 layout/base/PresShell.h
--- a/layout/base/PresShell.h	Tue Mar 25 08:09:49 2025 +0000
+++ b/layout/base/PresShell.h	Tue Mar 25 08:09:57 2025 +0000
@@ -914,6 +914,11 @@
     return aForPage ? mCanvasBackground.mPage : mCanvasBackground.mViewport;
   }
 
+  nscolor GetCSSSpecifiedCanvasBackground(bool aForPage) const {
+    const auto& canvas = GetCanvasBackground(aForPage);
+    return canvas.mCSSSpecified ? canvas.mColor : 0;
+  }
+
   struct CanvasBackground {
     // The canvas frame background for the whole viewport.
     SingleCanvasBackground mViewport;