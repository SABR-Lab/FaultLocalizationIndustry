# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: testing/web-platform/tests/scheduler/post-task-multiple-scheduler-order.window.js
# Commit: 41d90165c19a
# Full Hash: 41d90165c19adfa61486484b7ffb5ccab599461c
# Author: Sean Feng <sefeng@mozilla.com>
# Date: 2025-04-01 09:40:15
# Regressor Bug: 1920115
# File Overlap Count: 2
# Description:
#   Bug 1920115 - Allow selecting tasks from all existing web schedulers for main thread r=jjaschke,smaug
#   
#   This is a missing step in the current implementation. When we about
#   to select a task, it should pick one from all scheduled tasks, including
#   the ones scheduled by different schedulers.
# ==============================================================================

diff -r 2b4f7ebdb0b7 -r 41d90165c19a testing/web-platform/tests/scheduler/post-task-multiple-scheduler-order.window.js
--- /dev/null	Thu Jan 01 00:00:00 1970 +0000
+++ b/testing/web-platform/tests/scheduler/post-task-multiple-scheduler-order.window.js	Mon Mar 31 19:49:03 2025 +0000
@@ -0,0 +1,37 @@
+// META: title=Scheduler: Tasks run order when multiple scheduler interfere the same event loop
+promise_test(async t => {
+
+  const iframe = document.createElement("iframe");
+
+  iframe.onload = async function() {
+    const runOrder = [];
+    const tasks = [];
+
+    tasks.push(window.scheduler.postTask(function(){runOrder.push("outer")}));
+    tasks.push(iframe.contentWindow.scheduler.postTask(function(){runOrder.push("inner")}));
+
+    await Promise.all(tasks);
+
+    assert_equals(runOrder.toString(),'outer,inner');
+  }
+
+  document.body.appendChild(iframe);
+}, 'Test scheduler.postTask() from two different schedulers with the same priority will run based on their enqueue order.');
+
+promise_test(async t => {
+  const iframe = document.createElement("iframe");
+
+  iframe.onload = async function() {
+    const runOrder = [];
+    const tasks = [];
+
+    tasks.push(window.scheduler.postTask(function(){runOrder.push("outer")}));
+    tasks.push(iframe.contentWindow.scheduler.postTask(function(){runOrder.push("inner")}, {priority: "user-blocking"}));
+
+    await Promise.all(tasks);
+
+    assert_equals(runOrder.toString(),'inner,outer');
+  }
+
+  document.body.appendChild(iframe);
+}, 'Test scheduler.postTask() from two different schedulers with different priorities will run based on their priorities');
