# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: dom/workers/remoteworkers/RemoteWorkerDebuggerParent.cpp
# Commit: 07833c96f60c
# Full Hash: 07833c96f60cba643d641aaa04709574e7d69bb9
# Author: Eden Chuang <echuang@mozilla.com>
# Date: 2025-03-26 09:13:23
# Regressor Bug: 1899503
# File Overlap Count: 4
# Description:
#   Bug 1899503 - P3 PRemoteWorkerDebugger binding implementation. r=asuth
#   
#   This patch implements the PRemoteWorkerDebugger binding.
#   
#   We are trying to keep the same logic debugger registration/unregistration to minimize the difference between the local debugger(the debugger on the content process main thread)
# ==============================================================================

diff -r df07abc82381 -r 07833c96f60c dom/workers/remoteworkers/RemoteWorkerDebuggerParent.cpp
--- a/dom/workers/remoteworkers/RemoteWorkerDebuggerParent.cpp	Tue Mar 25 14:33:27 2025 +0000
+++ b/dom/workers/remoteworkers/RemoteWorkerDebuggerParent.cpp	Tue Mar 25 14:33:28 2025 +0000
@@ -4,6 +4,7 @@
 
 #include "RemoteWorkerDebuggerParent.h"
 #include "mozilla/dom/WorkerDebuggerManager.h"
+#include "mozilla/dom/WorkerPrivate.h"
 
 namespace mozilla::dom {
 
@@ -35,6 +36,10 @@
     listener->OnClose();
   }
 
+  if (CanSend()) {
+    Unused << SendUnregisterDone();
+  }
+
   return IPC_OK();
 }
 
@@ -106,6 +111,15 @@
 }
 
 NS_IMETHODIMP
+RemoteWorkerDebuggerParent::GetIsRemote(bool* aResult) {
+  AssertIsOnMainThread();
+  MOZ_ASSERT_DEBUG_OR_FUZZING(aResult);
+
+  *aResult = true;
+  return NS_OK;
+}
+
+NS_IMETHODIMP
 RemoteWorkerDebuggerParent::GetIsInitialized(bool* aResult) {
   AssertIsOnMainThread();
   MOZ_ASSERT_DEBUG_OR_FUZZING(aResult);