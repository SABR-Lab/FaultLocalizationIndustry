# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: dom/workers/sharedworkers/SharedWorkerOp.cpp
# Commit: 07833c96f60c
# Full Hash: 07833c96f60cba643d641aaa04709574e7d69bb9
# Author: Eden Chuang <echuang@mozilla.com>
# Date: 2025-03-26 09:13:23
# Regressor Bug: 1899503
# File Overlap Count: 4
# Description:
#   Bug 1899503 - P3 PRemoteWorkerDebugger binding implementation. r=asuth
#   
#   This patch implements the PRemoteWorkerDebugger binding.
#   
#   We are trying to keep the same logic debugger registration/unregistration to minimize the difference between the local debugger(the debugger on the content process main thread)
# ==============================================================================

diff -r df07abc82381 -r 07833c96f60c dom/workers/sharedworkers/SharedWorkerOp.cpp
--- a/dom/workers/sharedworkers/SharedWorkerOp.cpp	Tue Mar 25 14:33:27 2025 +0000
+++ b/dom/workers/sharedworkers/SharedWorkerOp.cpp	Tue Mar 25 14:33:28 2025 +0000
@@ -82,6 +82,10 @@
 
   MOZ_ASSERT(aState.is<Running>() || IsTerminationOp());
 
+  if (mOpArgs.type() == SharedWorkerOpArgs::TSharedWorkerThawOpArgs) {
+    aOwner->SetIsThawing(true);
+  }
+
   RefPtr<SharedWorkerOp> self = this;
   RefPtr<RemoteWorkerChild> owner = aOwner;
 
@@ -97,6 +101,11 @@
         }
 
         self->StartOnMainThread(owner);
+        if (self->mOpArgs.type() ==
+            SharedWorkerOpArgs::TSharedWorkerThawOpArgs) {
+          owner->SetIsThawing(false);
+          owner->RunAllPendingOpsOnMainThread();
+        }
       });
 
   MOZ_ALWAYS_SUCCEEDS(SchedulerGroup::Dispatch(r.forget()));
@@ -151,6 +160,12 @@
   } else {
     MOZ_CRASH("Unknown SharedWorkerOpArgs type!");
   }
+
+#ifdef DEBUG
+  if (!mStarted) {
+    mStarted = true;
+  }
+#endif
 }
 
 void SharedWorkerOp::Start(RemoteWorkerNonLifeCycleOpControllerChild* aOwner,
