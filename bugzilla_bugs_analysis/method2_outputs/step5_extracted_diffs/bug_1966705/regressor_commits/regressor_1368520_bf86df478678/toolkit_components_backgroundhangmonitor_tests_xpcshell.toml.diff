# ==============================================================================
# REGRESSOR COMMIT DIFF
# ==============================================================================
# File: toolkit/components/backgroundhangmonitor/tests/xpcshell.toml
# Commit: bf86df478678
# Full Hash: bf86df478678a7ee898a8237f1b1ccd3b6b50595
# Author: Florian Quèze <florian@queze.net>
# Date: 2025-04-18 09:12:23
# Regressor Bug: 1368520
# File Overlap Count: 2
# Description:
#   Bug 1368520 - enable BHR on Android, r=dthayer.
#   
#   Differential Revision: https://phabricator.services.mozilla.com/D245610
# ==============================================================================

diff -r ccf3c458ee30 -r bf86df478678 toolkit/components/backgroundhangmonitor/tests/xpcshell.toml
--- a/toolkit/components/backgroundhangmonitor/tests/xpcshell.toml	Thu Apr 17 20:44:26 2025 +0000
+++ b/toolkit/components/backgroundhangmonitor/tests/xpcshell.toml	Thu Apr 17 20:50:17 2025 +0000
@@ -1,12 +1,11 @@
 [DEFAULT]
 
 ["test_BHRObserver.js"]
-# BHR is disabled on android and outside of nightly
+# BHR is disabled outside of nightly
 support-files = ["child_cause_hang.js"]
 run-sequentially = "very high failure rate in parallel"
 skip-if = [
   "debug",
-  "os == 'android'",
   "os == 'mac' && os_version == '10.15' && processor == 'x86_64' && opt", # Bug 1417723
   "os == 'mac' && os_version == '11.20' && arch == 'aarch64' && opt", # Bug 1417723
   "os == 'mac' && os_version == '14.70' && processor == 'x86_64' && opt", # Bug 1417723