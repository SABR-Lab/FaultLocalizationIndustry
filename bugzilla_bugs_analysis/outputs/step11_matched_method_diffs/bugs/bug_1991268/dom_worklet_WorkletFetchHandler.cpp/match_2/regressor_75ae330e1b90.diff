diff -r c48ad530cfe2 -r 75ae330e1b90 dom/worklet/WorkletFetchHandler.cpp
--- a/dom/worklet/WorkletFetchHandler.cpp	Wed Sep 24 18:03:30 2025 +0000
+++ b/dom/worklet/WorkletFetchHandler.cpp	Wed Sep 24 18:03:30 2025 +0000
@@ -220,15 +220,6 @@
 
   request->mBaseURL = mURI;
   request->OnFetchComplete(mResult);
-
-  if (NS_FAILED(mResult)) {
-    if (request->IsTopLevel()) {
-      request->LoadFailed();
-    } else {
-      request->Cancel();
-    }
-  }
-
   moduleLoader->RemoveRequest(mURI);
   return NS_OK;
 }
