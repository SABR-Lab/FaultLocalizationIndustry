diff -r e1c5be04b3a0 -r cc6bc73fbbfd dom/worklet/WorkletFetchHandler.cpp
--- a/dom/worklet/WorkletFetchHandler.cpp	Wed Sep 24 23:17:54 2025 +0000
+++ b/dom/worklet/WorkletFetchHandler.cpp	Thu Sep 25 05:12:32 2025 +0300
@@ -220,6 +220,15 @@
 
   request->mBaseURL = mURI;
   request->OnFetchComplete(mResult);
+
+  if (NS_FAILED(mResult)) {
+    if (request->IsTopLevel()) {
+      request->LoadFailed();
+    } else {
+      request->Cancel();
+    }
+  }
+
   moduleLoader->RemoveRequest(mURI);
   return NS_OK;
 }